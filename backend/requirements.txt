# Production dependencies - DO NOT include -r directives here to avoid circular deps
# Use this file ONLY when installing manually with pip
# For Docker builds, use requirements-bedrock.txt, requirements-local.txt, or requirements-full.txt directly

# Base dependencies
fastapi>=0.115.0
uvicorn>=0.30.0
python-dotenv>=1.0.0
pandas>=2.2.0
sqlalchemy>=2.0.0
python-jose[cryptography]>=3.3.0
argon2-cffi>=23.1.0
python-multipart>=0.0.9
authlib>=1.3.0
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-aws>=0.2.0
boto3>=1.35.0

# Bedrock-specific (default for production)
langchain-anthropic>=0.2.0
anthropic>=0.25.0